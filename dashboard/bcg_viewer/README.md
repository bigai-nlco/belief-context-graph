# Legacy stream viewer (DEPRECATED)

This directory (`belief_graph_stream_viewer.html`, `serve_viewer.py`,
`build_stream_manifest.py`) is the pre-dashboard artifact replay viewer.

Status: **deprecated** — keep as a read-only reference until the dashboard
reaches feature parity (see `dashboard/README.md` feature matrix). No new
features land here; removal happens in the step-16 compatibility window.
