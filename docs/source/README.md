# Documentation source

These Markdown files are the editable source for the offline BCG documentation.
The browser-ready site is compiled into `../assets/pages.js` and opened through
`../index.html`.

After editing, run:

```bash
python3 ../check_offline_docs.py
```
